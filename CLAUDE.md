# معروضك — ملف السياق

> يُقرأ تلقائياً في بداية كل جلسة داخل هذا المستودع.
> غرضه: أن تبدأ أي محادثة جديدة بكامل السياق بلا شرح من المستخدم.

---

## ما هذا المشروع

منصة SaaS عربية لإنشاء المعاريض والخطابات الرسمية. تُجري **مقابلة ذكية**
مع المستخدم (سؤال واحد في كل شاشة، بأسئلة تتفرّع حسب إجاباته)، ثم يصوغ
الذكاء الاصطناعي خطاباً رسمياً **من إجاباته وحدها**.

**اللغة:** كل شيء بالعربية — الواجهة، الرسائل، التعليقات، الوثائق، رسائل
الالتزام. المستخدم يتواصل بالعربية.

---

## المبدأ الذي لا يُساوَم عليه

> **الذكاء الاصطناعي كاتب، لا مصدر معلومات.**

كل حقيقة في المعروض يجب أن تكون قابلة للتتبّع إلى إجابة قدّمها المستخدم.
لا أسماء ولا أرقام ولا مبالغ ولا تواريخ ولا أنظمة قانونية مخترعة.

الضوابط في `src/services/ai/guardrails.ts` **مثبّتة في الشيفرة** ولا تُقرأ من
قاعدة البيانات — لو أمكن تعديلها من لوحة التحكم لانهار ضمان المنتج.
انظر `docs/AI_SYSTEM.md §4`.

---

## البنية

```
src/services/    ❤️ منطق الأعمال النقي — لا يستورد next/* ولا Prisma
                 questions · templates · ai · credits · storage · analytics
src/lib/         البنية التحتية — db · auth · security · api · logging
src/features/    وحدات المنتج — auth · interview · letters · admin · marketing
src/app/         الصفحات ومسارات API
prisma/          المخطط · الترحيلات · البذور
tests/           unit + integration (على PostgreSQL حقيقية)
docs/            11 وثيقة — اقرأ ARCHITECTURE.md أولاً
```

**قاعدة معمارية:** `src/services/**` نقي وقابل للاختبار بلا إطار ولا قاعدة
بيانات. Prisma لا يُستدعى إلا داخل `src/lib/db/repositories/**`.

---

## أوامر أساسية

```bash
npm run db:start      # PostgreSQL محلية (بيانات في ~/.maroudak)
npm run dev
npm run verify        # typecheck + lint + test + build — بوابة كل تغيير
npm run preflight     # فحوص ما قبل النشر
npm run check:env     # يتحقق من حراس إعدادات الإنتاج
```

⚠️ لا تشغّل `npm run build` وخادم التطوير معاً — كلاهما يكتب في `.next`
ويتلفانه، وعلى ويندوز يقفل خادم التطوير محرك Prisma فيفشل البناء.

---

## حالة المشروع

**مكتمل ويعمل:** الرحلة الكاملة (اختيار جهة ← مقابلة بمنطق شرطي ← مراجعة ←
توليد ← محرر ← تصدير) · لوحة إدارة كاملة · 116 اختباراً · جاهزية إنتاج
(بيئات منفصلة · ترحيلات · فحص صحي · سجلات معقّمة · CI).

**البيانات:** 28 جهة · 21 نوع طلب · 98 سؤال · 15 قاعدة شرطية · 8 قوالب ·
43 موجّهاً · 4 خطط.

### لم يُنفَّذ عمداً في v1

| البند | ملاحظة |
|---|---|
| بوابة الدفع | البنية جاهزة لإضافتها |
| فرض الحصة الشهرية | الرصيد يعمل، لكن `lettersPerMonth` غير مفروض |
| إرسال البريد | روابط الاستعادة تُطبع في سجل الخادم |
| رفع المرفقات | طبقة التخزين جاهزة (`src/services/storage`) |
| اختبارات E2E | `playwright.config.ts` غير مكتوب |
| تحديد المعدّل عبر Redis | في الذاكرة — **لا يكفي لنشر متعدد النسخ** |

---

## 🚧 المهمة المعلّقة: النشر

المشروع **لم يُنشر بعد**. ما جُهّز وما تبقّى:

| الخطوة | الحالة |
|---|---|
| الشيفرة على GitHub (`toy79790/maaroodak`، خاص) | ✅ |
| الترحيلات ومتغيرات البيئة وأمر البناء | ✅ |
| `.env.production.local` بقيم جاهزة | ✅ (محلي، مستثنى من Git) |
| حجز `maroody.com` | ❌ متاح وقت الفحص، غير محجوز |
| قاعدة Neon | ❌ |
| النشر على Vercel | ❌ |
| ربط الدومين | ❌ |

**الترتيب مهم:** انشر أولاً على `maaroodak.vercel.app` بـ
`CANONICAL_HOST_MODE=none`، ثم اربط الدومين، ثم **أعد البناء** بالقيم
النهائية — `NEXT_PUBLIC_APP_URL` يُدمج وقت البناء لا وقت التشغيل.

الخطوات الكاملة: `docs/DEPLOYMENT.md`.

---

## قيود بيئة التطوير

هذه أُصلحت بحلول مقصودة — لا تُعِد كسرها:

1. **مسار المشروع يحتوي أحرفاً عربية.** ويندوز يمرّر مسارات الملفات
   التنفيذية بترميز WIN1256، فيرفضها PostgreSQL المُهيّأ بـ UTF-8.
   الحل: `scripts/db-server.ts` ينسخ الثنائيات إلى `~/.maroudak` (مسار
   لاتيني) ويشغّلها من هناك. انظر `DECISIONS.md #D-023`.

2. **أدوات الكتابة تحوّل `\uXXXX` إلى محارف فعلية.** لهذا يفحص
   `src/lib/utils/arabic.ts` المحارف غير المرئية بنقطة الترميز رقمياً لا
   بأنماط تحتوي المحرف نفسه.

3. **PowerShell يضيف BOM** مع `Out-File -Encoding utf8`. ملف ترحيل بـ BOM
   يفشل بخطأ صياغة غامض. استخدم Node لكتابة ملفات SQL.

4. **Node مثبت عبر winget** ولا يظهر في PATH الافتراضي للجلسة. اسبق أوامر
   PowerShell بتحديث PATH من متغيرات النظام.

---

## أسلوب العمل المتوقّع

- **تحقّق، لا تفترض.** المستخدم يقدّر الفحص الفعلي: شغّل، افتح المتصفح،
  اقرأ الـ DOM. حدث سابقاً أن قال «تم الرفع» ولم يكن قد تمّ — الفحص كشفه.
- **صرّح بما لم يُنفَّذ أو يُختبر.** لا تقل «يعمل» عمّا لم تشغّله.
- **`npm run verify` قبل إعلان الإتمام.**
- **لا نطاق ولا سرّ داخل الشيفرة.** كله من متغيرات البيئة.
- **قرارات تقنية غير محدّدة في الطلب** تُسجَّل في `docs/DECISIONS.md` برقم
  جديد مع البديل المرفوض وسببه.

---

## الوثائق

| الملف | متى تقرؤه |
|---|---|
| `ARCHITECTURE.md` | قبل أي تغيير بنيوي — الطبقات وحدود الوحدات |
| `AI_SYSTEM.md` | قبل لمس أي شيء يخص الذكاء الاصطناعي |
| `DATABASE.md` | المخطط والفهارس وسياسة الترحيل |
| `SECURITY.md` | نموذج التهديد والضوابط |
| `DECISIONS.md` | 30 قراراً تقنياً مع أسبابها — **اقرأه قبل «تحسين» شيء يبدو غريباً** |
| `DEPLOYMENT.md` | النشر وربط الدومين |
| `PRODUCTION_CHECKLIST.md` | قبل الإطلاق |
